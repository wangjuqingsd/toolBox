<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.ytg.toolBox.dao.UserDao">
    <insert id="save" >
        insert into hs_user(user_name, password) values (#{param1},#{param2})
    </insert>
</mapper>